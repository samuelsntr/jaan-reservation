---
deployment:
  tasks:
    - export DEPLOYPATH=/home/u1376460/jaanreservation-api
    - /bin/rsync -av --delete server/ $DEPLOYPATH
    - cd $DEPLOYPATH && npm install --production
    - touch $DEPLOYPATH/tmp/restart.txt
